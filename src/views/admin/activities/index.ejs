<%- include('../layouts/admin', { title: 'Activities Management', style: '',
script: '', content: `
<div class="container-fluid">
  <div class="row">
    <div class="col-12">
      <div class="card">
        <div
          class="card-header d-flex justify-content-between align-items-center"
        >
          <h3 class="card-title">Activities Management</h3>
          <a href="/admin/activities/create" class="btn btn-primary">
            <i class="fas fa-plus me-1"></i>Add New Activity
          </a>
        </div>
        <div class="card-body">
          <div class="table-responsive">
            <table class="table table-bordered table-hover">
              <thead class="table-light">
                <tr>
                  <th>Title</th>
                  <th>Category</th>
                  <th>Schedule</th>
                  <th>Location</th>
                  <th>Image</th>
                  <th>Status</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                ${(activities && activities.length > 0) ?
                activities.map((activity) => `
                <tr>
                  <td>${activity.title}</td>
                  <td>${activity.category}</td>
                  <td>${activity.schedule}</td>
                  <td>${activity.location}</td>
                  <td>
                    ${activity.image ? `<img
                      src="${activity.image}"
                      alt="Activity Image"
                      class="img-thumbnail"
                      style="max-width: 100px"
                    />` : `<span class="text-muted">No image</span>` }
                  </td>
                  <td>
                    <span
                      class="badge bg-${activity.active ? 'success' : 'danger'}"
                    >
                      ${activity.active ? 'Active' : 'Inactive'}
                    </span>
                  </td>
                  <td>
                    <div class="btn-group">
                      <a
                        href="/admin/activities/edit/${activity._id}"
                        class="btn btn-sm btn-info"
                      >
                        <i class="fas fa-edit"></i> </a
                      >&nbsp;
                      <button
                        type="button"
                        class="btn btn-sm btn-danger"
                        onclick="deleteActivity('${activity._id}')"
                      >
                        <i class="fas fa-trash"></i>
                      </button>
                    </div>
                  </td>
                </tr>
                `).join('') : `
                <tr>
                  <td colspan="7" class="text-center">No activities found</td>
                </tr>
                `}
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<<<<<<< HEAD
<script>
  function deleteActivity(id) {
    if (confirm("Are you sure you want to delete this activity?")) {
      fetch("/admin/activities/" + id, {
        method: "DELETE",
      })
        .then((response) => response.json())
        .then((data) => {
          if (data.success) {
            window.location.reload();
          } else {
            alert("Error deleting activity");
          }
        })
        .catch((error) => {
          console.error("Error:", error);
          alert("Error deleting activity");
        });
    }
  }
</script>
`}) %> ======= >>>>>>> 86e7c92ba509e7187111bbe295b59371a142d105
